export interface BandDetailRequestTemplate {
    name: string,
    genres: string[],
    active: string,
    members: string[],
    samplevids: string[],
    exmembers: string[]
    imagedata: string,
}